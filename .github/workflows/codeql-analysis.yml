name: "CodeQL Analysis"

on:
    push:
        branches: ["master"]
    pull_request:
        branches: ["master"]
    schedule:
        - cron: "0 3 * * 0"

jobs:
    analyze:
        name: Analyze
        runs-on: ubuntu-latest
        permissions:
            actions: read
            contents: read
            security-events: write

        strategy:
            fail-fast: false
            matrix:
                language: ["python"]

        steps:
            - name: Checkout repository
              uses: actions/checkout@v3

            - name: Initialize CodeQL
              uses: github/codeql-action/init@v2
              with:
                  languages: ${{ matrix.language }}
                  queries: +security-extended,security-and-quality,performance

            - name: Cache CodeQL database
              uses: actions/cache@v3
              with:
                  path: /home/runner/work/_temp/codeql_databases
                  key: ${{ runner.os }}-codeql-${{ matrix.language }}-${{ hashFiles('**/*') }}
                  restore-keys: |
                      ${{ runner.os }}-codeql-${{ matrix.language }}-

            - name: Perform CodeQL Analysis
              uses: github/codeql-action/analyze@v2

            - name: Check for outdated dependencies
              run: |
                  pip install safety
                  safety check

            - name: Notify on failure
              if: failure()
              uses: actions/github-script@v6
              with:
                  github-token: ${{secrets.GITHUB_TOKEN}}
                  script: |
                      github.rest.issues.create({
                        ...context.repo,
                        title: 'CodeQL Analysis Failed',
                        body: `The CodeQL analysis has failed for commit: ${context.sha}`
                      });
